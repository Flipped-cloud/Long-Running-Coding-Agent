# Workflow Service

A small, file-backed Python workflow scheduler.

The current implementation supports creating, retrieving, and listing tasks.
Run the public regression suite with:

```bash
python -m pytest -q
```

