# Toy Repository

This repository intentionally contains a small implementation bug in `calculator.py`.

Task:

```text
Fix the implementation bug in calculator.py so that all tests pass.
Inspect the existing code before editing it and run the test suite after the change.
```

Run tests:

```bash
python -m pytest -q
```
