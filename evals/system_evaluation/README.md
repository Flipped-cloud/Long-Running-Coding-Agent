# System Evaluation

Runs isolated local trials across completion-only, legacy command, contract verification, and contract plus generated-test conditions. It uses local fixtures and does not require network access or Docker.

```bash
longrun-agent eval run --manifest evals/system_evaluation/config.yaml
```
